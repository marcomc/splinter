---
- name: "Loading settings from profiles"
  include_vars: "{{ item }}"
  with_fileglob:
    - "{{ splinter_dir + '/profiles/' + base_profile + '/*.yml' if base_profile|length>0 }}"
    - "{{ splinter_dir + '/profiles/' + role_profile + '/*.yml' if role_profile|length>0 }}"
